import React from 'react';

import Button from '../../../components/Button';

const Login = () => {
  return (
    <div>
      <h4>Login</h4>
      <Button>Pulsar</Button>
    </div>
  );
};

export default Login;
